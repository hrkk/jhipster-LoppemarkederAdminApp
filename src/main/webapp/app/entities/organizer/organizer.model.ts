import { Marked } from '../marked';
export class Organizer {
    constructor(
        public id?: number,
        public name?: string,
        public email?: string,
        public marked2?: Marked,
    ) {
    }
}
